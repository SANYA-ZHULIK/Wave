const startButton =
    document.getElementById(
        'startButton'
    );


startButton.addEventListener(
    'click',
    startApp
);


function startApp() {

    /*
       Здесь позже будет
       переход на основной экран Wave.
    */

    alert(
        'Добро пожаловать в Wave!'
    );

}